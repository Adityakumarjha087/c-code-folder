#include <bits/stdc++.h>

using namespace std;

int main(){
    // hello world
    cout << "hello" << endl;
    // INput and output
    int a;
    cin >> a;
    cout << a << endl;
    /* using namespace std; → Allows using cout instead of std::cout.
    main() → Entry point of the program.
    cout << "Hello, World!"; → Prints output to the console.
    return 0; → Ends the program successfully.
    */
   // datatype
   int age = 21;  
    cout << "Age: " << age << endl;  

    // Floating-point data type  
    float height = 5.9;  
    cout << "Height: " << height << " feet" << endl;  

    // Double data type (more precision than float)  
    double pi = 3.1415926535;  
    cout << "Value of Pi: " << pi << endl;  

    // Character data type  
    char grade = 'A';  
    cout << "Grade: " << grade << endl;  

    // Boolean data type  
    bool isStudent = true;  
    cout << "Is Student? " << isStudent << endl;  

    // String data type  
    string name = "John Doe";  
    cout << "Name: " << name << endl; 
    if else
    int a = 2;
    if(a>10)return 0;
    else return 1;

    // for loop
    int a = 20;
    for(int i = 0;i<a;i++){
        cout << i << endl;
    }

    // while loop
    int i = 0;
    while (i<a)
    {
        cout << i << endl;
        i++;
    }
    
    // function 
}